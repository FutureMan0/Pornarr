/**
 * Generated from `openapi.json`. Replaced wholesale by `make openapi`.
 *
 * The API contract is the boundary between backend and frontend work, which is
 * why this package is generated rather than written: a hand-maintained client
 * drifts from the server and nobody notices until runtime.
 */
export const PACKAGE_ROLE = "api-client" as const;
